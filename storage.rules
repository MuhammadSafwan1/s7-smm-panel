rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow all operations for development
    match /{allPaths=**} {
      allow read: if true;
      allow write: if true;
      allow delete: if true;
    }
  }
}
